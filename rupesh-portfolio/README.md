# Rupesh Portfolio (React + TypeScript + Tailwind)

## Setup (local)

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start dev server:
   ```bash
   npm start
   ```

This project uses Create React App with TypeScript and Tailwind CSS.
