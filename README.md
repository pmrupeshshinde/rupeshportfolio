# Rupesh Portfolio — CRA + TypeScript + Tailwind

## Quick start (local)

1. Install dependencies
   ```bash
   npm install
   ```
2. Start dev server
   ```bash
   npm start
   ```
3. Open http://localhost:3000
